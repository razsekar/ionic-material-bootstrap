import { ConsultationsFilterPipe } from './consultations-filter.pipe';

describe('ConsultationsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ConsultationsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
